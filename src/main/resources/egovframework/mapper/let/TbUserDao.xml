<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rds.adams.web.core.dao.TbUserDao">
    <select id="selectUserByUserId" parameterType="com.rds.adams.web.core.dto.TbUserDto" resultType="com.rds.adams.web.core.dto.TbUserDto">
        SELECT
			   USER_ID
			   , USER_NAME
			   , PASSWORD
			   , TO_CHAR(CREATED_DATE, 'YYYYMMDD') AS CREATED_DATE
			   , CREATED_BY
		  FROM TB_USER
		 WHERE 1=1
		   AND USER_ID = #{userId}
    </select>
    
    <insert id="insertUser" parameterType="com.rds.adams.web.core.dto.TbUserDto">
		INSERT INTO TB_USER (
			USER_ID
			, USER_NAME
			, PASSWORD
			, CREATED_DATE
			, CREATED_BY
		) VALUES (
			#{userId}
			, #{userName}
			, #{password}
			, SYSDATE
			, #{createdBy}
		)
	</insert>
</mapper>