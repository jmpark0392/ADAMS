<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rds.adams.web.core.dao.FileMntDao">
    <select id="selectList" parameterType="com.rds.adams.web.core.dto.SearchTxtDto" resultType="com.rds.adams.web.core.dto.UplFileDto">
		SELECT
		       SEQ_NO
		     , FILE_NM
		     , DB_ID
		     , TBL_ID
		     , FILE_DSC
		     , FILE_DEL_YN
		     , UI_SEL_YN
		     , USE_YN
		     , VRF_USE_YN
		     , REG_USRID
		     , REG_DT
		  FROM TB_UPL_FILE_LST
         WHERE 1=1
         <if test='searchTxt != ""'>
           AND (
                   FILE_NM  LIKE CONCAT_WS('%', #{searchTxt}, '%')
                OR TBL_ID   LIKE CONCAT_WS('%', #{searchTxt}, '%')
                OR FILE_DSC LIKE CONCAT_WS('%', #{searchTxt}, '%')
                )
         </if>
    </select>
</mapper>